[project]
name = "trade-analysis"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "jsonschema>=4.26.0",
    "pandas>=3.0.5",
    "pyyaml>=6.0.3",
    "flask>=3.0.0",   # 只读 Web UI（scripts/ui）
]

[dependency-groups]
dev = [
    "pytest>=9.1.1",
]

# 环境
.venv/
__pycache__/
*.pyc
.pytest_cache/

# 外部工具状态
.workbuddy/
.opencode/

# 系统
.DS_Store

# 派生数据库（可由 data/raw 重跑管线重建；raw 源数据入库管理）
data/market.db
data/market.db.bak_*
data/backups/
# 采集落盘原始 CSV（按 §2.2 不可变，但不入 git——大且可由 MCP 重建）
data/raw/
# 生成产物（可由 daily pipeline + 排期卡 skill 重生成；不入 git）
reports/
cards/

# SQLite WAL 临时文件
data/market.db-shm
data/market.db-wal

# LLM 打标底稿/标签工作目录（agent/skill 通道，不入 git）
data/llm_tags/

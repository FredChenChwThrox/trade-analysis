# AI Agent 通用编程与架构指导原则

## 1. 核心目标

编写和修改代码时，以最低长期维护成本为目标，交付：

- 清晰可理解的代码
- 稳定可扩展的架构
- 可验证的功能行为
- 与业务目标匹配的可靠性和性能

避免为了“先进设计”引入不必要的复杂度。

---

# 2. 开始开发前：理解问题

在修改代码前，AI 应明确：

## 业务目标

- 系统最终产生什么价值？
  - 单次结果
  - 数据处理
  - 统计分析
  - 自动决策
  - 用户交互

## 可靠性要求

明确：

- 数据丢失是否允许？
- 重复处理是否允许？
- 延迟是否敏感？
- 错误结果是否比无结果更危险？

技术方案必须匹配业务损失，而不是默认追求最高可靠性。

## 变化范围

识别：

- 哪些部分长期稳定；
- 哪些部分经常变化；
- 哪些能力应该扩展；
- 哪些内容应该保持简单。

---

# 3. 架构设计原则

## 小核心，大扩展

优先采用：

```
入口
 ↓
流程控制
 ↓
稳定接口
 ↓
业务模块
 ↓
外部依赖
```

原则：

- 核心流程保持稳定；
- 变化逻辑放入独立模块；
- 外部依赖通过适配层隔离；
- 避免业务逻辑污染框架。

---

## 修改优先级

需求变化时，按以下顺序寻找修改位置：

```
配置
 ↓
现有业务模块
 ↓
公共能力
 ↓
框架结构
```

越靠近核心框架：

- 影响范围越大；
- 验证要求越高；
- 修改理由越充分。

---

## 避免过早抽象

不要：

- 为假设需求设计接口；
- 为一次复用创建复杂抽象；
- 为未来可能变化增加层级。

抽象应来自真实重复需求。

---

# 4. 模块和接口设计

## 模块原则

一个模块应该：

- 有明确职责；
- 有稳定输入输出；
- 可以独立测试；
- 可以独立替换。

避免：

- 模块之间互相调用形成复杂依赖；
- 创建无边界的公共工具模块；
- 将所有逻辑放入公共层。

---

## 接口原则

好的接口：

- 简单；
- 稳定；
- 语义明确；
- 不暴露内部实现。

避免：

- 使用 null、异常、特殊值表达不同含义；
- 修改已有接口语义；
- 让调用方依赖内部细节。

---

# 5. 可靠性设计

## 根据业务选择策略

不要默认所有系统都需要最高可靠性。

常见策略：

### 强一致型

适用于：

- 交易；
- 权限；
- 核心状态修改。

关注：

- 完整性；
- 幂等；
- 审计；
- 恢复。

### 结果导向型

适用于：

- 分析；
- 日志；
- 搜索；
- 数据处理。

关注：

- 结果可信；
- 避免系统性偏差；
- 保留必要证据。

### 尽力而为型

适用于：

- 缓存；
- 临时计算；
- 辅助功能。

关注：

- 简单；
- 快速失败。

---

## 异常处理

异常处理必须明确：

- 是否继续执行；
- 是否重试；
- 是否降级；
- 是否返回错误。

避免：

- 无意义捕获异常；
- 静默失败；
- 将程序错误伪装成正常结果。

---

# 6. 可维护性和可诊断性

代码应该帮助未来定位：

- 谁执行了；
- 输入是什么；
- 输出是什么；
- 为什么这样决定；
- 哪里失败。

日志原则：

- 描述行为，不描述情绪；
- 保留关键上下文；
- 避免泄露敏感信息；
- 高频路径避免大量日志。如必须记录，考虑burst rate等方式。

不要创建没人使用的：

- 指标；
- 告警；
- Dashboard；
- 恢复流程。

---

# 7. 性能原则

优化顺序：

1. 减少无意义计算；
2. 减少数据复制；
3. 缩小处理范围；
4. 批量处理；
5. 隔离慢操作；
6. 最后考虑并发优化。

不要：

- 为了“高性能”提前复杂化；
- 没有数据证明就引入缓存、并发、分布式。

并发设计必须考虑：

- 状态一致性；
- 生命周期；
- 错误恢复；
- 调试复杂度。

---

# 8. 编码风格

优先：

- 可读性 > 技巧；
- 简单修改 > 大规模重构；
- 显式流程 > 隐式行为；
- 组合 > 复杂继承。

函数：

- 一个函数解决一个明确问题；
- 根据业务阶段拆分；
- 避免超长逻辑。

配置：

- 可变内容进入配置；
- 稳定规则保留代码；
- 配置错误尽早发现。

注释：

解释：

- 为什么这么设计；
- 哪些约束不能破坏；
- 哪些取舍经过考虑。

不要解释代码表面行为。

---

# 9. AI Agent 修改代码流程

## 修改前

AI 应确认：

1. 需求属于哪一层？

   - 配置
   - 业务逻辑
   - 公共能力
   - 框架

2. 当前代码是否已有类似实现？

3. 是否存在兼容性要求？

4. 最小修改范围是什么？

---

## 修改原则

默认：

- 先理解，再修改；
- 优先局部修改；
- 不顺手重构；
- 不引入新框架；
- 不改变已有语义。

---

## 验证原则

AI 应优先：

- 添加测试；
- 验证边界条件；
- 检查接口兼容；
- 检查配置变化；
- 检查异常路径。

测试失败时先判断原因：

- 代码错误；
- 测试过期；
- 需求变化；
- 环境问题。

不要为了测试通过修改正确行为。

---

# 10. AI 与人的职责

AI：

负责：

- 大规模代码分析；
- 自动化修改；
- 测试补充；
- 一致性检查。

人：

负责：

- 业务理解；
- 架构取舍；
- 风险判断；
- 最终 Review。

原则：

> AI 提升开发效率，人负责决定正确方向。

---

# 总结

优秀的软件设计不是最大化技术能力，而是在业务目标、复杂度和长期维护成本之间取得平衡。

AI Agent 应：

- 理解业务损失；
- 控制设计复杂度；
- 保持接口稳定；
- 优先简单可靠方案；
- 通过验证保证质量。

让 AI 扩大工程能力，而不是扩大系统复杂度。
